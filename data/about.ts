export const aboutData = {
  quote: "Reliable systems over impressive demos. Understand the data before you model it. The pipeline is the product.",
  principles: [
    "Debug the pipeline before the model — garbage in, garbage out is not a cliché, it's a failure mode",
    "Coordinate systems are not optional — a single basis mismatch breaks downstream reconstruction",
    "Validation gates prevent silent failures — schema enforcement catches what tests miss",
    "Preprocessing is where the leverage lives — 80% of ML engineering is data preparation",
    "Ship working systems, not notebook prototypes — batch scripts, CLIs, and structured reports > interactive cells",
    "Target integrity is non-negotiable — label corruption is the silent killer of ML projects",
  ],
  currentFocus: "Currently focused on: SMPL/SMPL-H pipeline robustness for production motion datasets, production RAG architectures with NVIDIA NIM, and data-quality-first ML engineering with defensive pipelines.",
};